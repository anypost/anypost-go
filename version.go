package anypost

// Version is the SDK version, reported in the User-Agent header. It is the
// single source of truth: bump it, tag the commit `vX.Y.Z`, and push — the
// release workflow publishes the module proxy entry for the tag.
const Version = "1.0.0"
